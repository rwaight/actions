# Infra-sourced .github files

The following `.github` files are sourced from the 'infra' repo in the `infra/file-sync/dot_gh/` directory:

```bash
./dot_gh/
├── ISSUE_TEMPLATE
│   ├── config.yml.j2
│   └── other.yml.j2
├── PULL_REQUEST_TEMPLATE.md.j2
└── labeler.yml.j2
```
