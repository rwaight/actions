# Infra-sourced GitHub workflows

The following GitHub workflows are sourced from the 'infra' repo in the `infra/assets/gh_workflows/` directory:

```bash
./gh_workflows/
├── check-commits.yml
├── label-manager.yml
├── pr-labeler.yml
└── triage-stale.yml
```
