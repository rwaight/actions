name: get-commit-hash
description: placeholder
group: test
imported: false
local:
  author: rwaight
  modifications: true
specs:
  action_file: action.yml
  inputs: [gh-token, verbose]
  outputs: [current-version-tag, current-hash-suffix]
  runs:
    using: composite
tests:
  _comment: reserved for future use
