name: get-tag-version
description: placeholder
group: test
imported: false
local:
  author: rwaight
  modifications: true
specs:
  action_file: action.yml
  inputs: [verbose]
  outputs: [full-version-ref, major-ref, minor-ref, patch-ref, prerelease, is-prerelease, major-num, minor-num, patch-num]
  runs:
    using: composite
tests:
  _comment: reserved for future use
