name: set-build-type
description: placeholder
group: test
imported: false
local:
  author: rwaight
  modifications: true
specs:
  action_file: action.yml
  inputs: [gh-token, my_custom_input, action-verbose]
  outputs: [build-type]
  runs:
    using: composite
tests:
  _comment: reserved for future use
